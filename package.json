{
  "name": "quackprep",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "git submodule update --init --recursive --remote; npm install; npm run build:frontend && npm run start:backend",
    "dev": "concurrently --kill-others-on-fail --success=first \"npm run start:backend\" \"npm run dev:frontend\"",
    "build:frontend": "cd frontend && npm i && npm run build",
    "start:backend": "cd backend && npm i && npm start",
    "dev:frontend": "cd frontend && npm i && npm run dev"
  },
  "devDependencies": {
    "babel-plugin-module-resolver": "^5.0.2",
    "concurrently": "^9.0.1",
    "eslint": "^9.15.0",
    "eslint-plugin-promise": "^7.1.0",
    "playwright": "^1.49.0"
  },
  "dependencies": {
    "@babel/preset-env": "^7.25.7",
    "babel": "^5.8.38",
    "rand-seed": "^2.1.7",
    "maddox-js-funcs": "file:libs/maddox-js-funcs"
  }
}
